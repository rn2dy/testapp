- if @topics.count == 0
  .dashbox
    %h3 You're not involved in any topics. 
    %h1 Why wait? <b style="color:red">Get started!</b>
    %a#newTopic2{href: '#'} +New Topic
- else
  %ul.topicNavBox
    - @topics.each do |topic|
      %li{href: '#', data: {topic: "#{topic.id}"} }
        = link_to topic.name, topic
